#!/usr/bin/env bash
# Lists files in current dir and displays part after first dash

for file in *; do
  # Skip if it does not contain a dash
  if [[ "$file" == *-* ]]; then
    # Extract and print part after first dash
    echo "${file#*-}"
  fi
done
