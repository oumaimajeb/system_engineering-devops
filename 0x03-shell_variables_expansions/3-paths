#!/bin/bash
# Count the number of directories in PATH by splitting on colon,
# ignoring empty entries, then counting the rest.

# Using parameter expansion and tr + grep:
echo "$PATH" | tr ':' '\n' | grep -cve '^$'
